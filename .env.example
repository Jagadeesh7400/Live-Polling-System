# Environment Configuration for Live Polling System

# ===========================================
# ⚠️  IMPORTANT: UPDATE THESE URLs AFTER DEPLOYMENT
# ===========================================

# ===========================================
# VERCEL (FRONTEND) ENVIRONMENT VARIABLES
# ===========================================
# Add these in Vercel Dashboard → Settings → Environment Variables

# Backend API URL (Railway URL) - UPDATE WITH YOUR ACTUAL RAILWAY URL
VITE_API_URL=https://[YOUR-ACTUAL-RAILWAY-URL].railway.app

# Socket.io URL (same as API URL) - UPDATE WITH YOUR ACTUAL RAILWAY URL  
VITE_SOCKET_URL=https://[YOUR-ACTUAL-RAILWAY-URL].railway.app

# ===========================================
# RAILWAY (BACKEND) ENVIRONMENT VARIABLES  
# ===========================================
# Add these in Railway Dashboard → Variables

# Frontend URL (Vercel URL) - UPDATE WITH YOUR ACTUAL VERCEL URL
CLIENT_URL=https://[YOUR-ACTUAL-VERCEL-URL].vercel.app

# Server port (Railway auto-assigns, use ${{ PORT }})
PORT=${{ PORT }}

# Environment
NODE_ENV=production

# ===========================================
# LOCAL DEVELOPMENT ENVIRONMENT
# ===========================================
# For local development, create .env.local with:

# VITE_API_URL=http://localhost:5000
# VITE_SOCKET_URL=http://localhost:5000
# CLIENT_URL=http://localhost:3000
# PORT=5000
# NODE_ENV=development

# ===========================================
# DEPLOYMENT STEPS SUMMARY
# ===========================================

# 1. RAILWAY BACKEND:
#    - Connect GitHub repo
#    - Set root directory to "/server"  
#    - Add environment variables above
#    - Deploy automatically

# 2. VERCEL FRONTEND:
#    - Connect GitHub repo
#    - Set build command: "npm run build"
#    - Set output directory: "dist"
#    - Add environment variables above
#    - Deploy automatically

# 3. UPDATE URLS:
#    - Copy Railway URL and update VITE_API_URL in Vercel
#    - Copy Vercel URL and update CLIENT_URL in Railway
#    - Both platforms will auto-redeploy

# ===========================================
# EXAMPLE FINAL URLS
# ===========================================
# Frontend: https://live-polling-system-jagadeesh7400.vercel.app
# Backend:  https://live-polling-system-production.railway.app
# Health:   https://live-polling-system-production.railway.app/api/health

# Development Environment
REACT_APP_SERVER_URL=http://localhost:5000
NODE_ENV=development
PORT=5000
CLIENT_URL=http://localhost:3000

# Production Environment (Update these values)
# REACT_APP_SERVER_URL=https://your-backend-url.herokuapp.com
# NODE_ENV=production
# PORT=5000
# CLIENT_URL=https://your-frontend-url.vercel.app

# Database (if using external database)
# DATABASE_URL=your_database_connection_string

# Socket.io Configuration
# SOCKET_CORS_ORIGIN=https://your-frontend-url.vercel.app

# Security (Optional)
# JWT_SECRET=your_jwt_secret_key
# SESSION_SECRET=your_session_secret
